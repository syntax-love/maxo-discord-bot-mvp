services:
  - type: web
    name: discord-bot-dashboard
    runtime: node
    buildCommand: |
      cd dashboard && npm install && npm install vite @vitejs/plugin-react -D && npm run build
      cd .. && npm install
    startCommand: node server/app.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: SESSION_SECRET
        generateValue: true
      - key: DISCORD_CLIENT_ID
        sync: false
      - key: DISCORD_CLIENT_SECRET
        sync: false
      - key: DISCORD_REDIRECT_URI
        value: https://your-app.onrender.com/auth/discord/callback
      - key: MONGODB_URI
        sync: false
    staticPublishPath: dashboard/dist